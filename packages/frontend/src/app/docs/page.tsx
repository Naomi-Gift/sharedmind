import DocsPage from '@/components/DocsPage';

export const metadata = {
  title: 'SharedMind — Documentation',
  description: 'Everything you need to build with SharedMind — group pools, AI agents, x402 payments, streaks, and on-chain attestations.',
};

export default function Docs() {
  return <DocsPage />;
}
